Update ~/.claude/global-claude.md with the latest behavioral guidelines from https://github.com/longieirl/ai-tools.

Run:
```bash
curl -fsSL https://raw.githubusercontent.com/longieirl/ai-tools/main/.agent/global-claude.md \
  -o ~/.claude/global-claude.md
```

Report: confirm the file was updated and show the current section headings (grep for lines starting with `#`).
