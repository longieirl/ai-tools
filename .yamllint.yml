extends: default
rules:
  document-start: disable
  empty-lines:
    max: 2
  line-length:
    max: 160
    level: warning
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
ignore: |
  .git
  node_modules
